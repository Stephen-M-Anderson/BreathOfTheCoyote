using System.Collections;
using System.Collections.Generic;
using UnityEngine;

//Literally only used to represent that this is that particular gameobject
//Picked up this tendency from ECS, goal is to move this over to a tag, probably won't
public class FireballExplosion : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
